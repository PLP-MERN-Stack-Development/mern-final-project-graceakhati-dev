# ============================================
# Planet Path Frontend - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control
#
# Quick Start:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values
# 3. Never commit .env to git

# ============================================
# API Configuration
# ============================================
# Backend API URL
# Development: http://localhost:5000
# Production: https://your-backend-domain.com
# Note: Do NOT include /api suffix - it's added automatically
VITE_API_URL=http://localhost:5000

# ============================================
# Google OAuth Configuration (Optional)
# ============================================
# Get these from Google Cloud Console: https://console.cloud.google.com/
# Should match the CLIENT_ID used in backend
VITE_GOOGLE_CLIENT_ID=your-google-client-id

# Frontend redirect URI after Google OAuth
# Development: http://localhost:3001/auth/google/callback
# Production: https://your-frontend-domain.com/auth/google/callback
VITE_GOOGLE_REDIRECT_URI=http://localhost:3001/auth/google/callback

# ============================================
# Security Notes
# ============================================
# 1. Never commit .env file to version control
# 2. VITE_ prefixed variables are exposed to the browser
# 3. Do NOT put sensitive secrets in VITE_ variables
# 4. Only use VITE_ for non-sensitive configuration
# 5. All API keys and secrets should be on the backend
